# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# A GET request because that's what RiffRaff supports...
GET         /api/update/:org/:repo        controllers.Application.measureLag(org, repo, url, urlLabel: Option[String])
POST        /api/hooks/github             controllers.Application.githubHook(url, urlLabel: Option[String])

GET         /                             controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                 controllers.Assets.at(path="/public", file)
