# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# A GET request because that's what RiffRaff supports...
GET         /api/update/:org/:repo                      controllers.Application.updateRepo(org, repo, checkpoint)
POST        /api/update/:org/:repo/hooks/aws-sns        controllers.Application.snsHook(org, repo, checkpoint)
POST        /api/hooks/github                           controllers.Application.githubHook(checkpoint)

GET         /                                           controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                               controllers.Assets.at(path="/public", file)
