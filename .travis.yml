language: scala
scala:
- 2.11.12
jdk:
- openjdk8
sudo: false
cache:
  directories:
  - $HOME/.sbt
  - $HOME/.ivy2
before_cache:
  - find $HOME/.sbt -name "*.lock" -type f -delete
  - find $HOME/.ivy2/cache -name "*[\[\]\(\)]*.properties" -type f -delete

